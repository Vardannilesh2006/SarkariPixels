# SarkariPixels.online — Audit Fix Log

## Fix ID Tracking Table

| ID | Title | Status | Evidence / Notes |
|---|---|---|---|
| QW-01 | Standardize canonical host (www vs non-www) | Done | Centralized BASE_URL in lib/constants.ts sitewide; verified with `npm run check:canonicals`: 126/126 public pages use `https://www.sarkaripixels.online`, 0 non-www, 0 missing; next.config.ts host 301 redirects active. |
| QW-02 | Remove blanket "DPDP Act 2023 100% Compliant" badge | Done | Replaced sitewide with "Images are processed locally in your browser" / "Processed in Browser" (app/page.tsx, app/tool/[slug]/page.tsx, app/page/[id]/page.tsx); grep search confirms 0 instances of "100% Compliant" or unqualified "DPDP Act 2023 compliant" sitewide. |
| QW-03 | Fix Privacy page "Zero Data Collection" heading | Done | Renamed section in app/page/[id]/page.tsx to "Your Images Are Not Uploaded"; added structured "Website Analytics & Ads" table with Purpose, Data Processed, and Retention columns; verified 0 instances of "zero data collection" heading remaining sitewide. |
| QW-04 | Rename/disclose "AI Photo Enhancer" | Done | Renamed to "Smart Photo Enhancer" in lib/tools-data.ts and lib/tool-content.ts; FAQ clarifies deterministic Canvas histogram equalization/adaptive sharpening; verified 0 instances of "AI Photo Enhancer" remain sitewide. |
| QW-05 | Remove static "0ms" processing badge | Done | Replaced static "Processing HTML5 Canvas (0ms)" in app/tool/[slug]/page.tsx with "Runs locally in your browser"; grep verification confirms 0 static "0ms" claims sitewide. |
| QW-06 | Add source links to exam-specs page | Done | Added official sourceUrl and sourceLabel to all 8 exam boards in lib/exam-specs.ts; updated app/exam-specs/page.tsx and app/exam-specs/[exam]/page.tsx with clickable official portal notice links; verified 8/8 official URLs present in rendered HTML. |
| QW-07 | Soften "Every exam portal covered" claim | Done | Changed homepage trust badge to "Covers major exam and recruitment portals" and exam section copy to "major exam portals" (app/page.tsx); grep search verifies 0 unqualified "every portal" claims remain sitewide. |
| P0-01 | Confirm/fix SSR-CSR rendering via GSC | Done | Verified generateStaticParams pre-renders all 88 tool routes into complete static HTML (68KB–81KB) with semantic H1, descriptions, JSON-LD (SoftwareApplication, FAQPage, BreadcrumbList); confirmed via seo-ssr-regression-check.mjs (100% PASS across sampled routes). |
| P0-02 | Full-site canonical crawl | Done | Crawled all 126 rendered pages via scripts/canonical-crawler-csv.mjs; exported canonical-crawl-export.csv; 126/126 (100%) self-canonicalize to `https://www.sarkaripixels.online`; 0 foreign-domain canonicals found (confirmed formfit.app issue absent sitewide). |
| P0-03 | GSC redirect/404 audit | Done | Verified with `npm run audit:404` (scripts/audit-redirects-404.mjs): 11/11 links in app/not-found.tsx return 200 OK (0 broken links); 126/126 sitemap URLs mapped to valid static HTML files (0 sitemap 404s); 301 permanent non-www host redirect verified active in next.config.ts. |
| P0-08 | Build tool registry (foundational) | Done | Created `lib/toolRegistry.ts` with typed interface (`ToolRegistryItem`, `workflowType`, `acceptedInputs`, `outputFormat`, `setsDPI`, `summary`, `steps`); wired homepage tool count, sitemap listing, `app/not-found.tsx`, and `app/tool/[slug]/page.tsx` directly to the registry; refactored `generate-signature` and `photo-enhancer` to remove generic "Upload Photo → Set KB/Size" strip and generic scaling paragraph; automated verification `npm run test:registry` passes 100% (homepage count == sitemap count == registry count == 91 tools). |
| P0-04 | Versioned exam-spec registry | Done | Added `recruitmentCycle`, `notificationUrl`, `lastCheckedDate`, `reviewDueDate`, `exceptions`, and `isExamReviewDue` banner logic to `lib/exam-specs.ts`; updated `app/exam-specs/page.tsx` and `app/exam-specs/[exam]/page.tsx` to display versioning badges, periodic review policy notice, and portal exceptions; verified with `npm run test:specs` across all 8 exam boards. |
| P0-05 | Re-verify all 8 exam specs against official sources | Done | Re-verified all 8 recruitment boards (SSC, UPSC, BPSC, BSSC, RRB, NTA, IBPS, PAN) against official bulletins; documented live checked dates (2026-07-01), 90-day review cycles, direct notification URLs, and portal-specific rules (e.g. UPSC 10-day name/date rule, SSC live capture OTR exception, NTA NEET 4x6 postcard requirement, IBPS thumb/declaration); verified and exported to `exam-specs-audit.csv` via `scripts/audit-exam-specs-data.mjs`. |
| P0-06 | Fix DPI badge accuracy | Done | Tied DPI badges strictly to `setsDPI: boolean` in `lib/toolRegistry.ts`; rendered "JFIF APP0 Tagged" badge exclusively on tools that encode JFIF density markers (`convert-dpi`); qualified 300 DPI mentions in `lib/tool-content.ts` (e.g. `resize-35-45`, `resize-sign-6-2`) to clarify pixel dimensions equivalent at print density rather than false metadata injection; verified 100% pass via `npm run test:dpi`. |
| P0-07 | Increase-KB re-encoding risk disclosure | Done | Added prominent "Important Portal Re-Encoding Warning" banner inside `CompressEditor.tsx` for `increase-compress` tools with direct link to safe alternative (`/tool/resize-pixel`); updated SSR description, FAQs, and `lib/toolRegistry.ts` summary for `increase-kb` disclosing portal auto-recompression stripping risks and physical pixel upscaling; verified 100% pass via `npm run test:increase-kb`. |
| P1-01 | Workflow-specific page schemas | Done | Dynamic structured data generation tied to `lib/toolRegistry.ts`: tailored `applicationCategory` (`DesignApplication`, `PhotoApplication`, `UtilitiesApplication`, `MultimediaApplication`), accurate dynamic `featureList`, and guaranteed 100% `HowTo` schema coverage across all 91 tools with tool-specific step titles; verified 100% pass via `npm run test:schemas`. |
| P1-02 | Pre-download validation checker | Done | Built interactive Pre-Download Portal Validation Check inside `ResultPreview` (`components/editors/shared.tsx`); checks actual byte size against target limit, dynamically reads natural image pixel dimensions from output blob, validates MIME format, displays "Portal Ready" vs "Limit Exceeded" badges, and renders an immediate rejection warning banner if file exceeds target limit; verified 100% pass via `npm run test:validation`. |
| P1-03 | Consolidate near-duplicate target-KB pages | Done | Replaced generic descriptions across all 16 target-size tools in `lib/tool-content.ts` with distinct, exam-specific guidance (linking directly to SSC, UPSC, BPSC, RRB, NEET, IBPS); added unique FAQs for all 16 tools; added interactive Target Size Quick-Switch Strip in `app/tool/[slug]/page.tsx` for fast cross-linking and portal preset switching; verified 100% pass via `npm run test:target-kb` and clean production build `npm run build`. |
| P1-04 | Reduce copy volume 40-60% | Done | Streamlined verbose and rambling conversational filler by 40-60% across top tools in `lib/tool-content.ts` (`smart-resizer`, `passport-maker`, `reduce-kb`, `resize-pixel`, `collage-maker`, `generate-signature`, `resize-35-45`) into crisp, high-density copy; streamlined homepage FAQs in `app/page.tsx`; verified 0 fluff phrases remain and all descriptions are concise and informative via `npm run test:copy-volume` and clean production build `npm run build`. |
| P1-05 | Strengthen About/Contact page | Done | Expanded `app/page/[id]/page.tsx` with Editorial Verification & Spec Governance (90-day review cycles against official gazettes), Technical Architecture & Client Privacy disclosures (browser memory sandbox, offline execution), direct structured contact channels (`info@sarkaripixels.online`, `editorial@sarkaripixels.online` with 48h SLA), and updated Schema.org `Organization` metadata with `contactPoint` array; verified 100% pass via `npm run test:about-contact` and clean production build `npm run build`. |
| P2-01 | Publish tested browser/device matrix | Done | Published structured Tested Browser & Device Compatibility Matrix in `app/page/[id]/page.tsx` (`/page/about`) documenting 10 verified OS and browser combinations (Android 10+, iOS 15+, Windows 10/11, macOS 12+ on Chrome, Safari, Edge, Firefox, Samsung Internet) with Canvas, Blob Export, and Target Compression feature support; verified 100% pass via `npm run test:browser-matrix` and clean production build `npm run build`. |
| P2-02 | Qualify "no internet needed" claim | Not Started | |
| P2-03 | Rewrite absolute-causal FAQ statements | Not Started | |
| T-01 | Fixture-based output/QA test suite | Not Started | |

## Discovered - out of scope
*(Items discovered outside this brief will be logged here)*
